# Argon2id Provider Tests

`test-wasm-abi.cjs` is an isolated interoperability gate for the generated Node.js `wasm-bindgen` output. It is not a production browser test and does not register the provider with GoreeVault Web.
