<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex,nofollow,noarchive" />
    <meta name="referrer" content="same-origin" />
    <meta name="description" content="GoreeVault page not found." />
    <title>Page not found — GoreeVault</title>
    <link rel="stylesheet" href="{{urlpath}}/vw_static/404.css" />
</head>
<body>
    <a class="gv-skip-link" href="#gv-main">Skip to main content</a>

    <header class="gv-site-header">
        <div class="gv-header-inner">
            <a class="gv-brand" href="{{urlpath}}/" aria-label="GoreeVault home">
                <span class="gv-brand-mark">GoreeVault</span>
                <span class="gv-brand-product">Secure Vault</span>
            </a>
        </div>
    </header>

    <main id="gv-main" class="gv-container" tabindex="-1">
        <section class="gv-error-card" aria-labelledby="not-found-title">
            <p class="gv-eyebrow">Error 404</p>
            <h1 id="not-found-title">Page not found</h1>
            <p class="gv-lead">
                The page you requested is unavailable or the address is incorrect.
                No vault data was changed.
            </p>
            <a class="gv-primary-action" href="{{urlpath}}/">Return to GoreeVault</a>
        </section>
    </main>

    <footer class="gv-footer">
        <div class="gv-footer-inner">GoreeVault · A GoreeCloud service</div>
    </footer>
</body>
</html>
