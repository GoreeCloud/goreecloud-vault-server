name: GoreeVault Lockfile Refresh (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: Temporary workflow retired
    runs-on: ubuntu-24.04
    steps:
      - name: Retired
        run: |
          echo 'This temporary workflow is retired. Cargo.lock is committed and verified.'
          echo 'Do not use this workflow to update dependencies.'
